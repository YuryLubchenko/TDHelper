<Window x:Class="TDHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TDHelper"
        SizeToContent="Height"
        ResizeMode="CanMinimize"
        Width="300"
        Closing="MainWindow_OnClosing">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Label Grid.Column="0" Grid.Row="0" Content="Idle Time"/>
        <TextBox Grid.Column="1" Grid.Row="0" IsReadOnly="True" Name="IdleTextBox" Margin="10 5 10 5"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Started On"/>
        <TextBox Grid.Column="1" Grid.Row="1" IsReadOnly="True" Name="StartedOnTextBox" Margin="10 5 10 5"/>
    </Grid>
</Window>
